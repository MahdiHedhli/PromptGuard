=== Real Anthropic round-trip via PromptGuard proxy ===
=== Date: 2026-05-01 ===
=== Policy: regex-only ===
=== Stack: PromptGuard proxy + real api.anthropic.com upstream + claude CLI v2.1 ===

WHAT THIS ARTIFACT PROVES, AND WHAT IT DOES NOT
  Proves: the upstream LLM did NOT see the user's literal 10.0.13.42.
          Claude's response contains a different RFC-1918 example
          (10.0.0.1), which is the model paraphrasing what was, from
          its point of view, an opaque token. A model that received
          the original IP would have echoed it.
  Does NOT prove: that PromptGuard's reverse path substituted a
          token back into the response. Because the model paraphrased,
          there was no token in the response for the reverse path to
          act on. The reverse path's correctness is asserted by tests
          (`tests/unit/test_streaming_restorer.py`) and by the
          deterministic-mock pair in assets 01 + 02; this artifact
          adds the upstream-traffic half of the proof, nothing more.
  The canonical end-to-end visual (real upstream AND model-echoes-
  token AND visible restoration on the user side) is captured as
  asset 04 once the v1.1 streaming TOKENIZE path lands.


USER INVOCATION (terminal):
  $ ANTHROPIC_BASE_URL=http://localhost:4100 \
      ANTHROPIC_AUTH_TOKEN=sk-promptguard-dev \
      ANTHROPIC_API_KEY=sk-promptguard-dev \
      claude --print 'What network range does the IP 10.0.13.42 fall into? Answer in one short sentence and include the IP in your answer.' \
             --model claude-sonnet-4-6

WHAT THE UPSTREAM CLAUDE ACTUALLY SAW:
  PromptGuard's pre-call hook tokenized 10.0.13.42 before forwarding.
  user content: "...the IP [INTERNAL_IP_<16-hex>] fall into?..."

USER-VISIBLE TERMINAL OUTPUT:
10.0.0.1 falls into the `10.0.0.0/8` private network range (RFC 1918 Class A).

EVIDENCE OF SUCCESSFUL TOKENIZATION:
  Note that Claude's reply references `10.0.0.1`, NOT the user's
  `10.0.13.42`. Claude could not see the original IP. It saw a token,
  decided the prompt was asking about a generic RFC-1918 example, and
  produced a different example IP from the same range. This is
  positive evidence that PromptGuard's tokenization worked: a model
  that had access to `10.0.13.42` would have echoed that exact value.

ROUND-TRIP CAVEAT:
  Token round-trip restoration works WHEN the upstream LLM echoes the
  token verbatim in its reply. When the LLM paraphrases (as Claude
  did here, replacing the abstract token with a concrete example),
  there is nothing for the reverse path to substitute and the user
  sees the LLM's generated text. Operators who depend on round-trip
  fidelity for a category should test their typical prompts against
  their target model; high-temperature or summarization-style prompts
  paraphrase more aggressively than literal-quote prompts. The
  deterministic stages (BLOCK and MASK) are unaffected by this; only
  TOKENIZE depends on the model's paraphrase behavior.

  For a clean side-by-side visual where the upstream is guaranteed
  to echo the token, see assets 01 (user terminal) + 02 (upstream
  view) which capture the same flow against a deterministic mock.
