# Daily Report 2026-05-03

**Day 5 of 14 (covering Day 4 carryover + Day 5)** | **Status:** ON TRACK
**Phase:** Day 4 closure (claude CLI + LiteLLM ?beta=true) + Day 5 (policy config polish)
**Working session length:** ~3 hours

---

## Summary (60-second version)

Day 4 carryover and Day 5 deliverables both shipped in this session. LiteLLM `?beta=true` is unblocked via `allow_requests_on_db_unavailable` (DEC-015), claude CLI now talks through the proxy end-to-end. Mock-based round-trip captured as the headline blog visual: four random tokens reach the upstream, none of the originals, user sees their original IP and hostnames in the response. A fifth shipped policy `pentest-engagement.yaml` joins the four existing samples; an audit suite asserts every shipped policy still matches the locked default mapping in research-notes section 5. New-user simulation timed end-to-end at 161 seconds (well under the 5-minute target). Policy hot-reload shipped opt-in (DEC-016, stdlib polling, validates before swap, preserves the per-conversation TokenMap). 132 tests passing. Real-key live test still pending: `.env` only has `GITHUB_PAT`; `ANTHROPIC_API_KEY` was supposed to be added per the brief but isn't there. Mock-based capture serves as the v1 validation artifact.

## Completed today

- **Day 4 carryover.**
  - DEC-015: LiteLLM `general_settings.allow_requests_on_db_unavailable: true`. Applied to both `docker/litellm/config.yaml` and `docker/litellm/config-mock.yaml`. Documented as the right call for v1 because PromptGuard does not use LiteLLM's spend tracking, virtual keys, or audit DB.
  - claude CLI v2.1 now sends to `/v1/messages?beta=true`, fires through the PromptGuard pre-call hook, hits the upstream, returns through the post-call. Verified via the mock stack.
  - Two blog assets captured under `docs/blog-assets/` with a labeled `README.md` per the new asset-capture protocol:
    - [`01-tokenize-roundtrip-user-terminal.txt`](../docs/blog-assets/01-tokenize-roundtrip-user-terminal.txt): claude CLI session showing the user typed an IP and got their original IP back in the reply.
    - [`02-tokenize-roundtrip-upstream-view.json`](../docs/blog-assets/02-tokenize-roundtrip-upstream-view.json): mock-Anthropic introspection. Four PromptGuard tokens reached the upstream; the four corresponding originals (`claude.ai`, `settings.local`, `github.com`, `10.0.13.42`) never left the host. The user's email was MASKed (no ledger).
- **Day 5 work.**
  - Audit of shipped policies vs research-notes section 5 locked mapping, formalized as 11 unit tests in [`tests/unit/test_sample_policies.py`](../tests/unit/test_sample_policies.py). Default policy matches the locked mapping; every shipped policy BLOCKs all credential categories; healthcare-leaning MASKs all personal identifiers (no ledger); pentest-engagement BLOCKs customer infrastructure; regex-only correctly disables OPF/Presidio/llm_judge; v1 ships every policy with `llm_judge.enabled: false`.
  - New shipped policy: [`policies/pentest-engagement.yaml`](../policies/pentest-engagement.yaml) for external security testing under client NDA. BLOCKs customer infrastructure (domains, internal IPs, codenames, account numbers); MASKs personal identifiers; mirrors NDA-strict's overall posture with pentest-specific commentary.
  - [`docs/policy-schema.md`](../docs/policy-schema.md) extended with the override-mechanism section (no per-request override at v1, three documented escape hatches: `audit_only`, detector toggles, restart-with-different-file, plus DEC-016 hot reload), the schema-error format, and the at-startup validation behavior. Internally consistent: every documented field exists in `Policy`/`DetectorConfig`/`PolicyRule`; nothing implemented goes undocumented.
  - **New-user simulation.** Tore down the stack and removed the OPF model-cache volume to simulate first run, then `docker compose up -d --wait`. **161 seconds** (2:41) from clean state to all three services healthy with the default policy (regex + OPF + Presidio enabled). Verified the stack accepts a real request: AWS-key prompt returns 400 with the PromptGuard envelope. Within the 5-minute budget; subsequent boots reuse the cached volume and complete in seconds.
  - **OPF eager-load fix.** Found that `@app.on_event("startup")` was not firing reliably in our FastAPI version. Replaced with the modern `lifespan` async context manager. Eager load now starts on container boot; the docker healthcheck targets `/ready` (model loaded) instead of `/health` (server up) so `docker compose up -d --wait` actually waits until the OPF model is usable. Long `start_period` (300s) accommodates the first-run model download. Subsequent boots are seconds.
  - **README rewrite.** Day-5 brief asked for a 5-minute new-user path; the README now includes: `.env` setup instructions, expected first-boot duration (~3 min), air-gapped/restricted-egress fast path via `regex-only.yaml`, port-collision overrides, and a development section that points contributors at the mock-Anthropic profile for free integration testing.
  - **Policy hot-reload (DEC-016).** Shipped opt-in via `PROMPTGUARD_POLICY_RELOAD_INTERVAL_S`. Stdlib polling, no new deps. Validates the new YAML, re-probes detector readiness (DEC-009 hard-fail at runtime), atomically swaps `Policy`/`DetectionPipeline`/`ActionEngine` references, preserves the per-conversation TokenMap. 6 unit tests cover mtime change, unchanged mtime, schema-error rejection, OPF-unavailable rejection, TokenMap survival across swap, and the background-thread smoke test.

## Decisions made autonomously

| ID | Title | One-line summary |
|---|---|---|
| [DEC-015](../decisions/015-litellm-allow-requests-on-db-unavailable.md) | LiteLLM `allow_requests_on_db_unavailable: true` | One-line config change; lets `?beta=true` claude CLI requests reach our pre-call hook. PromptGuard does not use LiteLLM's spend tracking. v1.1 revisit if PromptGuard moves to shared/team mode. |
| [DEC-016](../decisions/016-policy-hot-reload-opt-in-polling.md) | Policy hot-reload via opt-in mtime polling | Stdlib polling on a daemon thread. Off by default. Validates new YAML, re-probes detectors, atomic swap, preserves TokenMap. Off-path performance unaffected. |

## Issues hit

| Issue | Resolution / workaround | Time cost |
|---|---|---|
| `.env` did not contain `ANTHROPIC_API_KEY` despite the brief asserting it would. The file was last modified Apr 30 14:02 (the original create) and only contains `GITHUB_PAT`. | Continued via the mock-Anthropic stack for round-trip validation. The headline blog visual is captured but on mock data. Real-key swap is one env var if/when the key lands. Flagged here so the operator (Mahdi) knows. | ~5 min triage |
| OPF eager-load not firing on boot. `@app.on_event("startup")` is deprecated and our FastAPI version was silently skipping it; the `_load_thread` global was None for the entire container lifetime. /ready stayed at "loading" forever. | Replaced with `@contextlib.asynccontextmanager async def lifespan` registered at `FastAPI(...,lifespan=lifespan)`. Confirmed by re-running the new-user simulation: model loads during the docker `start_period`, /ready flips to 200, LiteLLM hard-fail check at proxy startup passes. | ~30 min |
| Container healthcheck on OPF was `/health` (server up). `docker compose up -d --wait` was returning before OPF was actually usable, then LiteLLM's `depends_on: condition: service_healthy` would let LiteLLM start, then LiteLLM's pre-flight DEC-009 check would hard-fail because /ready was still 503. | Switched OPF healthcheck to `/ready` (model loaded). Long `start_period: 300s` to accommodate first-run model download. Subsequent boots are seconds (cached volume). | ~10 min |
| New-user simulation needed precise wall-clock timing of "fresh checkout to working stack". | Used `T0=$(date +%s); docker compose up -d --wait; T1=$(date +%s)` after `docker compose down -v` and confirmed by sending an AWS-key request afterward. Recorded 161 seconds. | n/a (intentional measurement) |

## Blockers requiring Mahdi input

**Soft blocker (does not block Day 6 progress):**

`ANTHROPIC_API_KEY` not in `.env` yet. The Day-4 brief said it would be there for the real-key claude CLI capture. The mock-based capture is in `docs/blog-assets/`; if you want a real-Anthropic capture as well, drop the key into `.env` and I can rerun the capture with no other code change.

## Trajectory assessment

ON TRACK. Day 4-5 gates are met:

- LiteLLM `?beta=true` unblocked.
- claude CLI session through proxy: works (mock); ready to repeat against real upstream.
- TOKENIZE round-trip preserves user-visible content: confirmed both unit and live (mock).
- No PII in upstream request body: confirmed via `_test/last_received` introspection.
- All Day 1-4 tests still pass.
- Sample policies audited; pentest-engagement added; schema doc complete.
- One-line install validated end-to-end; **161s from clean state**.
- Hot-reload nice-to-have shipped. ~90 minutes elapsed; well under the 4-hour timebox.

The eager-load fix and the healthcheck change are both quality improvements that ship today and unblock the deploy-in-minutes UX target. Day 6-7 work (adapter framework completion, Purview/ICAP stubs) starts from a solid base.

## Code / artifact highlights (blog post fodder)

- **The blog headline visual is captured** at [`docs/blog-assets/01-...txt`](../docs/blog-assets/01-tokenize-roundtrip-user-terminal.txt) and [`02-...json`](../docs/blog-assets/02-tokenize-roundtrip-upstream-view.json). The split-screen "user typed `10.0.13.42`, upstream got `[INTERNAL_IP_02ef306c7b29d172]`, user got `10.0.13.42` back" is precisely the demo CISOs nod at. Add a real-Anthropic version when the API key is available.
- **161 seconds from `docker compose up -d --wait` to working proxy on a clean checkout.** First boot downloads the OPF model from HuggingFace, eager-loads it via FastAPI's lifespan API, runs the docker healthcheck against `/ready` (so the orchestration layer actually waits), and reports healthy when LiteLLM has the policy loaded and OPF is ready to detect. Worth a paragraph in the post about the "deploy in minutes" claim landing under three minutes on a clean machine.
- **Policy hot-reload behavior.** The validation gate (re-probe detector readiness on swap) is the kind of thing security folks notice. A policy edit that flips `detectors.opf.enabled: true` is rejected at swap time if OPF is not currently `/ready`, with the same actionable error message as the startup hard-fail. Operators get told what to fix; the proxy keeps serving the previous policy until they fix it. See DEC-016.
- **Pentest-engagement policy.** New shipped sample. Customer infrastructure (domains, internal IPs, codenames, account numbers) BLOCKs outright; personal identifiers MASK. The "we're under NDA so even the token-map ledger is a contractual record" rationale is documented in the YAML comments and lifts directly into a blog sidebar.
- **Schema-error UX rendered concretely:**
  ```
  policy file: /app/policies/default.yaml
    at line 7, column 12 (rules.2.action): Input should be 'BLOCK',
    'MASK', 'TOKENIZE' or 'ALLOW'; got 'REJECT'
  ```
  Operators do not need to read pydantic stack traces. Worth a code-block in the policy section of the post.

## Tomorrow's plan (Day 6-7, adapter framework completion)

1. Formalize `DetectorAdapter` ABC with the documented contract (deterministic-ish output, span-based output format, category-mapping discipline). Audit existing Regex / OPF / Presidio adapters against the contract.
2. Wire `LLMJudgeDetector` as a class with default-off behavior; Day-8 fills in real implementation.
3. Audit `LocalYAMLPolicy` and `GitManifestPolicy` against `PolicyAdapter` contract.
4. PurviewDLPPolicy stub: write a sample SIT export fixture under `tests/fixtures/purview/`, build the adapter that loads + translates to PromptGuard categories, integration test that the resulting policy applies end-to-end.
5. ICAPPolicy stub: same shape, sample ICAP request/response fixtures.
6. `docs/adapters.md` documenting the framework.
7. Validation gates: a user can swap policy source by changing one config line; sample import files documented; tests that load Purview/ICAP fixtures produce equivalent action decisions to the YAML versions.
8. Daily report.

## Metrics snapshot

- **Tests:** 132 / 132 passing (115 from Day 1-3 + 17 new today: 11 sample-policy audit, 6 policy reloader). 10 skipped cleanly when stack is down.
- **Detection benchmark vs corpora:** still scheduled for Day 9.
- **Latency:**
  - In-process pipeline + action engine: unchanged (last measured 0.154ms avg).
  - End-to-end through stack: not re-measured today (no policy or hook code touched the request path; only adjacent infra).
  - Hot-reload poll cycle: ~0.05ms per cycle when no change; full validation + swap on change is ~10-15ms (dominated by pydantic + YAML parse).
- **Cold-start:** **161 seconds** from `docker compose up -d --wait` to all-services-healthy on first boot (clean cache volume). Subsequent boots: ~10s.
- **Code coverage:** not measured this session.
- **LoC added today:** ~750 across .py, .yaml, .md.
- **Dependencies added today:** **none** (DEC-016 explicitly chose stdlib polling over `watchdog` to avoid a dep).

## Open questions for the planning chat

- **Real-Anthropic capture.** The blog-asset placeholder is there; ready to rerun against a real key when one's in `.env`. If you want it next session, drop the key in.
- **DEC-016 default-off vs default-on.** I shipped hot-reload as default-off (operator opts in via env var). If you want operators to get hot-reload "for free" on the deploy-in-minutes path, flip the default to a small interval (e.g. 5s). Soft-checkpoint flag: this would add a polling thread to every install; CPU cost is trivial but it's still a behavior change.
- **`audit_only: true` mode.** Schema field exists from Day 1. The full audit-log writer is not yet wired (Day 6-7's adapter-framework work is the natural place to fold this in). Confirming you want the audit log as a Day 6-7 deliverable; if you want it explicitly carved out, say so.
